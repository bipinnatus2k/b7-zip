[package]
name = "bit7z-ffi"
version = "0.1.2"
# C++ wrappers are compiled directly with the `cc` crate; autocxx is not
# used (autocxx 0.30 is unmaintained and incompatible with current rustc).
edition = "2021"
license.workspace = true

build = "build.rs"

[dependencies]

[build-dependencies]
cc = "1"
